{
  "name": "@agentform/registry",
  "version": "0.1.0",
  "private": true,
  "description": "Agentform module and plugin registry — local, signed module resolution and lockfile support.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@agentform/core": "workspace:*",
    "@agentform/diagnostics": "workspace:*",
    "@agentform/ir": "workspace:*",
    "@agentform/plugin-sdk": "workspace:*",
    "@agentform/schema": "workspace:*",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1"
  }
}
