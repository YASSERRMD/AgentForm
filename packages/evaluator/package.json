{
  "name": "@agentform/evaluator",
  "version": "0.1.0",
  "private": true,
  "description": "Agentform evaluation engine — structural and dataset-driven test assertions.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@agentform/core": "workspace:*",
    "@agentform/ir": "workspace:*",
    "@agentform/parser": "workspace:*",
    "@agentform/runtime": "workspace:*",
    "ajv": "^8.20.0",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  }
}
