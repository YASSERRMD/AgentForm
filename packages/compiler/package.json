{
  "name": "@agentform/compiler",
  "version": "0.1.0",
  "private": true,
  "description": "Agentform compiler — IR-to-target-framework code generation pipeline.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@agentform/diagnostics": "workspace:*",
    "@agentform/ir": "workspace:*",
    "@agentform/plugin-sdk": "workspace:*",
    "@agentform/policy": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.1"
  }
}
