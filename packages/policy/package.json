{
  "name": "@agentform/policy",
  "version": "0.1.0",
  "private": true,
  "description": "Agentform policy engine — built-in and custom policy evaluation.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@agentform/diagnostics": "workspace:*",
    "@agentform/schema": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.1"
  }
}
